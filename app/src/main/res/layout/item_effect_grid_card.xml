<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="4dp">

    <!-- Square Thumbnail Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardEffectThumb"
        android:layout_width="match_parent"
        android:layout_height="104dp"
        app:cardBackgroundColor="#1A2234"
        app:cardCornerRadius="18dp"
        app:cardElevation="0dp"
        app:strokeColor="#28334E"
        app:strokeWidth="1dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/ivEffectThumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />

            <!-- AM Circular Pro/Featured Badge in top-right -->
            <ImageView
                android:id="@+id/ivEffectBadge"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="top|end"
                android:layout_margin="6dp"
                android:src="@drawable/ic_tool_effects"
                android:visibility="gone"
                app:tint="#00E5BC" />
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Effect Title -->
    <TextView
        android:id="@+id/tvEffectName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="8dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="2"
        android:minLines="1"
        android:text="Effect Name"
        android:textColor="#FFFFFF"
        android:textSize="12sp" />

</LinearLayout>
